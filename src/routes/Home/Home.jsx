import React, { Fragment } from 'react'
import Hero from '../../components/Hero'

const Home = ({ props }) => {
	return (
		<Fragment>
			<Hero props={props} />
		</Fragment>
	)
}

export default Home

